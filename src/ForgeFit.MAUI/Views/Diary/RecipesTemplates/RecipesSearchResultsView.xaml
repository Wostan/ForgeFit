<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:maui="clr-namespace:LocalizationResourceManager.Maui;assembly=LocalizationResourceManager.Maui"
             xmlns:addFood="clr-namespace:ForgeFit.MAUI.ViewModels.Diary.AddFood"
             xmlns:recipes="clr-namespace:ForgeFit.MAUI.ViewModels.Diary.Recipes"
             xmlns:recipesTemplates="clr-namespace:ForgeFit.MAUI.Views.Diary.RecipesTemplates"
             x:DataType="addFood:AddFoodPageViewModel"
             x:Class="ForgeFit.MAUI.Views.Diary.RecipesTemplates.RecipesSearchResultsView"
             x:Name="ThisView">
    <ContentView.Content>
        <CollectionView ItemsSource="{Binding RecipesVM.SearchResults}">

            <CollectionView.EmptyView>
                <Grid>
                    <VerticalStackLayout Spacing="10"
                                         VerticalOptions="Center"
                                         HorizontalOptions="Center"
                                         Opacity="0.5"
                                         IsVisible="False">
                        <VerticalStackLayout.Triggers>
                            <DataTrigger TargetType="VerticalStackLayout"
                                         Binding="{Binding RecipesVM.SearchText}"
                                         Value="">
                                <Setter Property="IsVisible" Value="True" />
                            </DataTrigger>
                        </VerticalStackLayout.Triggers>
                        <Image Source="food.svg"
                               MaximumWidthRequest="100"
                               MaximumHeightRequest="100"
                               Aspect="AspectFit">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{StaticResource TextSecondary}" />
                            </Image.Behaviors>
                        </Image>
                        <Label Style="{StaticResource LabelBody}"
                               Text="{maui:Translate Recipe_EmptyList}"
                               HorizontalOptions="Center" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="10"
                                         VerticalOptions="Center"
                                         HorizontalOptions="Center"
                                         Opacity="0.5">
                        <VerticalStackLayout.Triggers>
                            <DataTrigger TargetType="VerticalStackLayout"
                                         Binding="{Binding RecipesVM.SearchText}"
                                         Value="">
                                <Setter Property="IsVisible" Value="False" />
                            </DataTrigger>
                        </VerticalStackLayout.Triggers>
                        <Image Source="search.svg"
                               MaximumWidthRequest="100"
                               MaximumHeightRequest="100"
                               Aspect="AspectFit">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{StaticResource TextSecondary}" />
                            </Image.Behaviors>
                        </Image>
                        <Label Style="{StaticResource LabelBody}"
                               Text="{maui:Translate Recipe_NoResults}"
                               HorizontalOptions="Center" />
                    </VerticalStackLayout>

                    <ActivityIndicator IsRunning="{Binding RecipesVM.IsLoading}"
                                       IsVisible="{Binding RecipesVM.IsLoading}"
                                       VerticalOptions="Center"
                                       HorizontalOptions="Center"
                                       MaximumHeightRequest="50"
                                       MaximumWidthRequest="50"
                                       Color="{StaticResource Primary}" />
                </Grid>
            </CollectionView.EmptyView>

            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="0" />
            </CollectionView.ItemsLayout>

            <CollectionView.Header>
                <VerticalStackLayout>
                    <BoxView HeightRequest="160" BackgroundColor="Transparent" />

                    <Label Style="{StaticResource LabelH2}"
                           Text="{maui:Translate Tab_Recipes}"
                           TextColor="{StaticResource TextSecondary}"
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </CollectionView.Header>

            <CollectionView.Footer>
                <BoxView HeightRequest="60" BackgroundColor="Transparent" />
            </CollectionView.Footer>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="recipes:RecipeItemViewModel">
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems>
                                <SwipeItemView>
                                    <Grid ColumnDefinitions="*,*"
                                          WidthRequest="120"
                                          VerticalOptions="Fill">
                                        <Grid Grid.Column="0"
                                              BackgroundColor="{StaticResource DangerColor}"
                                              VerticalOptions="Fill"
                                              HorizontalOptions="Fill">
                                            <Image Source="delete.svg"
                                                   HorizontalOptions="Center"
                                                   VerticalOptions="Center" />
                                            <Grid.GestureRecognizers>
                                                <TapGestureRecognizer
                                                    Command="{Binding BindingContext.RecipesVM.DeleteRecipeCommand, Source={x:Reference ThisView}}"
                                                    CommandParameter="{Binding .}" />
                                            </Grid.GestureRecognizers>
                                        </Grid>
                                        <Grid Grid.Column="1"
                                              BackgroundColor="{StaticResource Primary}"
                                              VerticalOptions="Fill"
                                              HorizontalOptions="Fill">
                                            <Image Source="edit.svg"
                                                   HorizontalOptions="Center"
                                                   VerticalOptions="Center" />
                                            <Grid.GestureRecognizers>
                                                <TapGestureRecognizer
                                                    Command="{Binding BindingContext.RecipesVM.EditRecipeCommand, Source={x:Reference ThisView}}"
                                                    CommandParameter="{Binding .}" />
                                            </Grid.GestureRecognizers>
                                        </Grid>
                                    </Grid>
                                </SwipeItemView>
                            </SwipeItems>
                        </SwipeView.RightItems>

                        <VerticalStackLayout>
                            <recipesTemplates:RecipeItemCardView />

                            <BoxView HeightRequest="1"
                                     Color="{StaticResource BorderColor}"
                                     Margin="16,0"/>
                        </VerticalStackLayout>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentView.Content>
</ContentView>
