<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:behaviors="clr-namespace:ForgeFit.MAUI.Behaviors"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:maui="clr-namespace:LocalizationResourceManager.Maui;assembly=LocalizationResourceManager.Maui"
             xmlns:auth="clr-namespace:ForgeFit.MAUI.ViewModels.Auth"
             x:Class="ForgeFit.MAUI.Views.Auth.LoginPageView"
             x:DataType="auth:LoginPageViewModel"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False">
    <ContentPage.Content>
        <Grid RowDefinitions="*, Auto">
            <Grid.Padding>
                <OnPlatform x:TypeArguments="Thickness">
                    <On Platform="iOS" Value="30,70,30,40" />
                    <On Platform="Android" Value="30,50,30,20" />
                </OnPlatform>
            </Grid.Padding>

            <Border Grid.Row="0"
                    Style="{StaticResource InputWrapper}"
                    VerticalOptions="Start"
                    HorizontalOptions="End">
                <Grid ColumnDefinitions="Auto">
                    <Picker Grid.Column="0"
                            ItemsSource="{Binding Languages}"
                            SelectedItem="{Binding SelectedLanguage}"
                            ItemDisplayBinding="{Binding Name}"
                            VerticalOptions="Center"
                            MaximumWidthRequest="20"
                            BackgroundColor="Transparent"
                            TextColor="Transparent" />
                    <Image Grid.Column="0"
                           Source="language.svg"
                           MaximumHeightRequest="40"
                           MaximumWidthRequest="40"
                           Aspect="AspectFit"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"
                           ZIndex="1">
                        <Image.Behaviors>
                            <toolkit:IconTintColorBehavior TintColor="{StaticResource Primary}" />
                        </Image.Behaviors>
                    </Image>
                </Grid>
            </Border>

            <VerticalStackLayout Grid.Row="0"
                                 Spacing="25"
                                 VerticalOptions="Center">
                <VerticalStackLayout HorizontalOptions="Center" Spacing="20">
                    <Label Text="ForgeFit"
                           FontSize="50"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"
                           LineBreakMode="NoWrap"
                           x:Name="ForgeFitLabel"
                           FontFamily="MontserratSemiBold"
                           TextColor="{StaticResource Primary}"
                           Shadow="{StaticResource PrimaryGlow}" />

                    <Label Text="{maui:Translate WelcomeBack}"
                           HorizontalOptions="Fill"
                           HorizontalTextAlignment="Center"
                           LineBreakMode="NoWrap"
                           Style="{StaticResource LabelH1}" />
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="15">

                    <Border Style="{StaticResource InputWrapper}">
                        <Entry Text="{Binding Email}"
                               x:Name="EmailEntry"
                               Placeholder="{maui:Translate Placeholder_Email}"
                               behaviors:EntryAnimationBehavior.IsError="{Binding IsEmailError}"
                               IsEnabled="{Binding IsNotLoading}"
                               Keyboard="Email"
                               ReturnType="Next"
                               ClearButtonVisibility="WhileEditing">
                            <Entry.Behaviors>
                                <toolkit:EventToCommandBehavior EventName="TextChanged"
                                                                BindingContext="{Binding Path=BindingContext, Source={x:Reference EmailEntry}}"
                                                                Command="{Binding EntryChangedCommand}" />
                            </Entry.Behaviors>
                        </Entry>
                    </Border>

                    <Border Style="{StaticResource InputWrapper}">
                        <Entry Text="{Binding Password}"
                               x:Name="PasswordEntry"
                               Placeholder="{maui:Translate Placeholder_Password}"
                               behaviors:EntryAnimationBehavior.IsError="{Binding IsEmptyPasswordField}"
                               IsEnabled="{Binding IsNotLoading}"
                               IsPassword="True"
                               ReturnType="Done">
                            <Entry.Behaviors>
                                <toolkit:EventToCommandBehavior EventName="TextChanged"
                                                                BindingContext="{Binding Path=BindingContext, Source={x:Reference PasswordEntry}}"
                                                                Command="{Binding EntryChangedCommand}" />
                            </Entry.Behaviors>
                        </Entry>
                    </Border>
                </VerticalStackLayout>

                <Grid>
                    <Button Text="{maui:Translate SignInButton}"
                            Command="{Binding SignInCommand}"
                            behaviors:ButtonAnimationBehavior.IsLoading="{Binding IsLoading}" />

                    <ActivityIndicator IsRunning="{Binding IsLoading}" />
                </Grid>

                <Label Text="{Binding Error.Localized}"
                       Opacity="{Binding IsError}"
                       TextColor="{StaticResource DangerColor}"
                       HorizontalOptions="Center"
                       HorizontalTextAlignment="Center" />

            </VerticalStackLayout>

            <VerticalStackLayout Grid.Row="1"
                                 HorizontalOptions="Center">
                <Label Text="{maui:Translate DontHaveAnAccount}"
                       TextColor="{StaticResource TextSecondary}" />
                <Label Text="{maui:Translate SignUpButton}"
                       TextColor="{StaticResource Primary}"
                       FontAttributes="Bold"
                       HorizontalTextAlignment="Center">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToRegistrationCommand}" />
                    </Label.GestureRecognizers>
                </Label>
            </VerticalStackLayout>

        </Grid>
    </ContentPage.Content>
</ContentPage>
